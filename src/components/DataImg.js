import React from "react";

function DataImg({ imageUrl }) {
  return (
    <div className="Gambar1">
      <img className="img" src={imageUrl} alt="contact avatar" />
    </div>
  );
}

export default DataImg;
